{
  "name": "cordova-plugin-aws",
  "version": "0.2.1",
  "description": "Cordova Plugin for AWS",
  "config": {
    "vals": "AWS_REGION AWS_COGNITO_POOL_ID"
  },
  "scripts": {
    "postinstall": "add_plugin ${npm_package_config_vals}",
    "prestart": "web_inject ${npm_package_config_vals}"
  },
  "bin": {
    "add_plugin": "./scripts/add_plugin.js",
    "web_inject": "./scripts/web_inject.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fathens/Cordova-Plugin-AWS_Base.git"
  },
  "author": "f:athens",
  "license": "MIT",
  "dependencies": {
    "himalaya": "^0.2.2",
    "xml2js": "^0.4.17"
  }
}
